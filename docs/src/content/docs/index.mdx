---
title: SlashForge
description: Workflow slash commands for AI coding agents — /slashforge:setup, /slashforge:code, /slashforge:investigate.
template: splash
# Starlight renders "<page title> | <site title>", which on the home page gave
# the duplicated "SlashForge | SlashForge". Overridden so the tag carries the
# value proposition instead of repeating the name.
head:
  - tag: title
    content: SlashForge — workflow slash commands for AI coding agents
hero:
  # The lockup replaces the text title visually. The <h1> is kept in the DOM and
  # hidden with CSS rather than removed, so the page still has a heading for
  # screen readers and search engines.
  image:
    light: ../../assets/lockup-light.svg
    dark: ../../assets/lockup-dark.svg
    alt: SlashForge
  tagline: |
    Workflow slash commands for AI coding agents. Guardrails, not autocomplete.
    <span class="sf-support">Currently supports <strong>Claude Code</strong>. Cursor and Codex targets are planned.</span>
  actions:
    - text: Get started
      link: guides/introduction/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/rajdeepratan/SlashForge
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div class="sf-splash">

## Three commands

<CardGrid>
  <Card title="/slashforge:setup" icon="setting">
    One-time repo setup. Explores the codebase, asks clarifying questions, then
    generates `CLAUDE.md`, rules, skills, agents, commands, and hooks tailored
    to it.
  </Card>
  <Card title="/slashforge:code" icon="rocket">
    End-to-end development. Ten phases from plan to merged PR, with mandatory
    stop-and-confirm gates. Add `-quick` for lean mode on small changes.
  </Card>
  <Card title="/slashforge:investigate" icon="magnifier">
    Read-only research. Reproduces and root-causes a bug, then writes a findings
    report. Never touches your code.
  </Card>
</CardGrid>

</div>
